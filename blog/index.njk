---
layout: base.njk
lang: fr
noHero: true
altUrl: /en/blog/
title: Articles
description: Conseils en kinésithérapie et ostéopathie, actualités santé, par Andreea Almajan à Meudon Val Fleury.
permalink: /blog/
---
<main id="main-content">
  <section class="blog-wrap section-wrap">
    <h1 class="section-title">{{ site.articles[lang] }}</h1>
    <p class="blog-subtitle">{{ site.articles_subtitle[lang] }}</p>
    <div class="section-rule"></div>
    <ul class="article-list" role="list">
      {% set frPosts = collections.post | reverse %}
      {% for post in frPosts %}
        {% if post.data.lang == 'fr' %}
        <li class="article-card">
          <a href="{{ post.url }}" class="article-card__link">
            <div class="article-card__meta">
              <time datetime="{{ post.date | dateISO }}">{{ post.date | readableDate('fr') }}</time>
            </div>
            <h2 class="article-card__title">{{ post.data.title }}</h2>
            <p class="article-card__excerpt">{{ post.data.description }}</p>
            <span class="article-card__more">{{ site.read_more[lang] }} →</span>
          </a>
        </li>
        {% endif %}
      {% else %}
      <li>{{ site.no_articles[lang] }}</li>
      {% endfor %}
    </ul>
  </section>
</main>
